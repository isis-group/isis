Think about whether a -plugins package is necessary. All plugins link against
the shared lib anyway. If a new lib package is built the plugins get rebuilt as
well. If the shared lib interface changes the old plugins probably stop working
anyway...
